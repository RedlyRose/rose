{
  "name": "vaulthub-app",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "postbuild": "rm -rf .wrangler && echo \"export { default } from './server/entry.mjs';\" > dist/_worker.js && rm -f dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.2",
    "astro": "^6.0.5",
    "wrangler": "^4.74.0"
  }
}