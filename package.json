{
  "name": "vaulthub-app",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "rm -rf .wrangler dist",
    "build": "npm run prebuild && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "postbuild": "cp -rn dist/client/* dist/ 2>/dev/null || true && rm -rf dist/client && mv dist/server/entry.mjs dist/_worker.js 2>/dev/null || true && cp -r dist/server/chunks dist/ 2>/dev/null || true && rm -rf dist/server && rm -f dist/_worker.js.map"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.2",
    "@aws-sdk/client-s3": "^3.1010.0",
    "@aws-sdk/s3-request-presigner": "^3.1010.0",
    "astro": "^6.0.5",
    "wrangler": "^4.74.0"
  }
}
