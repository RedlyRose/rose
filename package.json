{
  "name": "vaulthub-app",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "postbuild": "echo \"export { default } from './server/entry.mjs';\" > dist/_worker.js && echo '{\"name\": \"vault-hub\", \"pages_build_output_dir\": \"dist\"}' > dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.2",
    "astro": "^6.0.5",
    "wrangler": "^4.74.0"
  }
}