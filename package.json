{
  "name": "vaulthub-app",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "postbuild": "cp -rn dist/client/* dist/ && rm -rf dist/client && mv dist/server/entry.mjs dist/_worker.js && cp -r dist/server/chunks dist/ && rm -rf dist/server"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.2",
    "astro": "^6.0.5",
    "wrangler": "^4.74.0"
  }
}